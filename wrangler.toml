name = "sarms"
compatibility_date = "2026-02-02"
main = "worker.js"

[assets]
directory = "./dist"
binding = "ASSETS"
not_found_handling = "single-page-application"
# Force Worker to run for every request so SPA fallback works on refresh
run_worker_first = true
